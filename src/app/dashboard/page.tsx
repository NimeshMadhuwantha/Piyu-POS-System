"use client";
import Link from "next/link";import {useOrders} from "@/hooks/use-data";import {formatLKR} from "@/lib/calculations";import {StatusBadge} from "@/components/status-badge";import {format,isToday} from "date-fns";
export default function Dashboard(){const{orders,loading}=useOrders();const today=orders.filter(o=>isToday(new Date(o.createdAtClient)));const stats=[{label:"Today's Orders",value:today.length},{label:"Today's Sales",value:formatLKR(today.filter(o=>o.orderStatus!=="Cancelled"&&o.orderStatus!=="Returned").reduce((s,o)=>s+o.grandTotal,0))},{label:"Pending",value:orders.filter(o=>["New","Confirmed"].includes(o.orderStatus)).length},{label:"Processing",value:orders.filter(o=>o.orderStatus==="Processing").length},{label:"Shipped",value:orders.filter(o=>o.orderStatus==="Shipped").length},{label:"Delivered",value:orders.filter(o=>o.orderStatus==="Delivered").length},{label:"Cancelled",value:orders.filter(o=>o.orderStatus==="Cancelled").length},{label:"Returned",value:orders.filter(o=>o.orderStatus==="Returned").length}];return <><div className="page-head"><div><h1>Dashboard</h1><span className="muted">Your order overview</span></div><Link className="btn" href="/orders/new">+ New Order</Link></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(145px,1fr))",gap:12,marginBottom:20}}>{stats.map(s=><div className="card" key={s.label}><div className="muted" style={{fontSize:12,fontWeight:700}}>{s.label}</div><div style={{fontSize:23,fontWeight:800,marginTop:9}}>{loading?"—":s.value}</div></div>)}</div><div className="card"><div className="page-head"><h2 className="section-title">Recent Orders</h2><Link href="/orders">View all</Link></div>{orders.length===0?<div className="muted">No orders yet. Create the first order to get started.</div>:<div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Date</th><th>Customer</th><th>Total</th><th>Status</th><th>Sync</th></tr></thead><tbody>{orders.slice(0,8).map(o=><tr key={o.id}><td><Link href={`/orders/${o.id}`}>{o.orderCode}</Link></td><td>{format(new Date(o.createdAtClient),"dd MMM, h:mm a")}</td><td>{o.customer.name}</td><td>{formatLKR(o.grandTotal)}</td><td><StatusBadge value={o.orderStatus}/></td><td>{o.pending?<span style={{color:"#b45309"}}>Waiting</span>:"Synced"}</td></tr>)}</tbody></table></div>}</div></>}
