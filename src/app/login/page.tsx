"use client";
import {useState} from "react";import {signInWithEmailAndPassword} from "firebase/auth";import {auth,firebaseConfigured} from "@/lib/firebase";import {useRouter} from "next/navigation";import {LockKeyhole} from "lucide-react";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();if(!firebaseConfigured){setError("Firebase is not configured. Add your values to .env.local.");return}setBusy(true);setError("");try{await signInWithEmailAndPassword(auth,email,password);router.replace("/dashboard")}catch{setError("Sign in failed. Check your email, password, and internet connection.")}finally{setBusy(false)}}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"linear-gradient(135deg,#eff6ff,#f8fafc)"}}><form className="card" onSubmit={submit} style={{width:"min(420px,100%)",padding:30}}><div style={{width:48,height:48,borderRadius:13,display:"grid",placeItems:"center",background:"#2563eb",color:"white",marginBottom:18}}><LockKeyhole/></div><h1 style={{margin:"0 0 7px"}}>Welcome to Piyu POS</h1><p className="muted" style={{margin:"0 0 24px"}}>Sign in with your authorized administrator account.</p><div style={{display:"grid",gap:15}}><label className="field">Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field">Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div style={{color:"#b91c1c",fontSize:13}}>{error}</div>}<button className="btn" disabled={busy}>{busy?"Signing in…":"Sign in"}</button></div><p className="muted" style={{fontSize:12,marginTop:20}}>A new device needs internet for its first login. An authorized installed device can reopen offline afterward.</p></form></main>}
