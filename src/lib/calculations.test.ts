import {describe,expect,it} from "vitest";import {calculateLineSubtotal,calculateTotals} from "./calculations";import {generateOrderCode} from "./order-code";import {orderFormSchema} from "./validation";
describe("order calculations",()=>{it("calculates line subtotal and prevents negatives",()=>{expect(calculateLineSubtotal(2,100,25)).toBe(175);expect(calculateLineSubtotal(1,10,20)).toBe(0)});it("calculates discounts, delivery, paid and COD balance",()=>{expect(calculateTotals([{quantity:2,unitPrice:500,discount:100}],50,200,250,"Cash on Delivery (COD)")).toEqual({itemsSubtotal:900,grandTotal:1050,amountPaid:250,balance:800,codAmount:800})});it("never returns NaN",()=>{expect(calculateTotals([{quantity:NaN,unitPrice:10,discount:0}]).grandTotal).toBe(0)})});
describe("validation and IDs",()=>{it("uses a collision-resistant readable format",()=>{expect(generateOrderCode(new Date(2026,7,7,10,45,12))).toMatch(/^ORD-20260807-104512-[A-Z2-9]{4}$/)});it("rejects an order without required client/item data",()=>{expect(orderFormSchema.safeParse({customer:{},items:[]}).success).toBe(false)})});
